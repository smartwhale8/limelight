# Release builds do not minify, so this file is a placeholder. If minification is enabled
# later, note that the app uses no reflection and no third-party runtime library, so the
# default Android rules should suffice.
